# Base URL of the deployed/local backend API (no trailing slash)
VITE_API_URL=http://localhost:4000

# WebSocket URL of the backend (ws:// locally, wss:// in production)
VITE_WS_URL=ws://localhost:4000
